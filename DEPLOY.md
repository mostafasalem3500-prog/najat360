# نشر NAJAT360 على رابط عام حقيقي — دليل تنفيذي مرحلي

هذا الدليل يفترض أنك فتحت هذا المجلد على جهازك (بعد فك ضغط الملف)، وأن لديك
Node.js مثبّتًا. لا شيء هنا يتطلب مني الوصول لأي حساب أو كلمة مرور خاصة بك —
كل خطوة تسجيل دخول تقوم بها أنت بنفسك في متصفحك.

---

## المرحلة 1 — رفع الكود إلى GitHub (٥ دقائق)

1. افتح https://github.com/new وأنشئ مستودعًا فارغًا جديدًا (مثلاً باسم
   `najat360`) — **لا تُفعّل** خيار "Add a README" حتى لا يتعارض مع الكود
   المرفق.
2. في هذا المجلد (يحتوي بالفعل على `git init` وأول Commit)، نفّذ:
   ```bash
   git remote add origin https://github.com/<اسم_حسابك>/najat360.git
   git branch -M main
   git push -u origin main
   ```
   سيطلب منك GitHub تسجيل الدخول في المتصفح (أو عبر GitHub CLI إن كان مثبتًا) —
   هذه الخطوة أنت من يقوم بها مباشرة.

---

## المرحلة 2 — قاعدة بيانات Postgres مجانية

اختر مزودًا واحدًا (كلاهما مجاني بدون بطاقة ائتمان لهذا الحجم):

- **Railway** (موصى به إن كان لديك حساب بالفعل — لاحظت وجود إعدادات Railway
  على جهازك): https://railway.app → New Project → Provision PostgreSQL.
  انسخ متغيّر `DATABASE_URL` من تبويب Variables.
- **Neon**: https://neon.tech → New Project → انسخ رابط الاتصال (Connection
  String) من الـ Dashboard.

احتفظ بقيمة `DATABASE_URL` جانبًا — ستحتاجها في المرحلتين 3 و4.

---

## المرحلة 3 — تطبيق مخطط قاعدة البيانات

من جهازك (وليس من هنا)، مع تثبيت `psql` أو باستخدام أداة المزوّد نفسه:

```bash
for d in prisma/migrations/*/; do
  psql "$DATABASE_URL" -f "$d/migration.sql"
done
node -e "require('ts-node/register'); " 2>/dev/null || true
npx tsx scripts/seed-demo.ts   # يزرع بيانات تجريبية اصطناعية بالكامل — لا بيانات حقيقية
```

(هذا نفس الأمر الموثّق في `README.md` تحت "Getting started" — لم يتغيّر شيء
هنا، فقط ننفّذه ضد قاعدة البيانات السحابية بدل المحلية.)

---

## المرحلة 4 — نشر التطبيق

### إذا اخترت Railway
1. من لوحة Railway، داخل نفس المشروع: New → GitHub Repo → اختر `najat360`.
2. Railway يكتشف Next.js تلقائيًا (Nixpacks) ويبني وينشر.
3. أضف متغيرات البيئة التالية في تبويب Variables لخدمة التطبيق:
   ```
   DATABASE_URL=<من المرحلة 2>
   APP_URL=<الرابط الذي يولّده Railway بعد أول نشر>
   DEMO_MODE=true
   ROUTING_PROVIDER=mock
   AUTH_SECRET=<أي نص عشوائي طويل تولّده بنفسك، مثل نتيجة: openssl rand -hex 32>
   ```
4. أعد النشر (Redeploy) بعد إضافة المتغيرات.

### إذا اخترت Vercel + Neon
1. https://vercel.com/new → Import Git Repository → اختر `najat360`.
2. في خطوة Environment Variables، أضف نفس المتغيرات أعلاه (مع `DATABASE_URL`
   من Neon).
3. اضغط Deploy.

---

## المرحلة 5 — التحقق وربط النتيجة بملفات العرض

بعد ظهور الرابط الحي (مثل `https://najat360.up.railway.app` أو
`https://najat360.vercel.app`):

1. افتح الرابط وتأكد أن الصفحة الرئيسية تظهر (شاشة اختيار الدور).
2. أرسل لي الرابط هنا في المحادثة.
3. سأتحقق منه، وأولّد كود QR حقيقيًا يشير إليه، وأحدّث عرض PowerPoint
   والملف التعريفي ليضمّا الرابط الحي الفعلي بدل الإشارة الحالية لعدم وجود
   نشر عام.

---

## ملاحظة مهمة قبل المشاركة الخارجية

`DEMO_MODE=true` يُفعّل مبدّل الأدوار التجريبي (Demo Role Switcher) وزر
إعادة التعيين — مناسب لعرض الهاكاثون فقط. البيانات بالكامل اصطناعية
(`synthetic: true` في كل صف)، لكن إن كنت تخطط لإبقاء الرابط متاحًا للعامة
لفترة طويلة بعد الهاكاثون، فكّر لاحقًا في تقييد الوصول (كلمة مرور بسيطة أمام
Vercel/Railway، أو إغلاق الرابط بعد التقييم) — هذا خارج نطاق هذه المرحلة لكن
يستحق الانتباه.
