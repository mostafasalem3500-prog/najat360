# NAJAT360 — تقرير المرحلة C6

التاريخ: 2026-08-28
يكمّل: `NAJAT360-تقرير-المرحلة-C5.md` (C1+C2+C3+C4+C5 مكتملة ومتحقق منها مسبقًا، 387 اختبارًا).

## الحالة: C6 (الإرسال المدرك للتغطية — H3 / P90 / Dispatch Score) مكتملة ومتحقق منها بالكامل

```
npm run verify
→ typecheck ✅  lint ✅ (0 أخطاء، 5 تحذيرات any موروثة من ملفات اختبار C5، غير متعلقة بهذه المرحلة)  ci:secret-scan ✅  ci:phase-gate-scan ✅  test ✅ (447/447)
```

طُبّق كل شيء فعليًا على نفس قاعدة PostgreSQL المحلية الحقيقية (migration جديد `0005_c6_coverage`)، ونُفّذ `npm run seed` الكامل حيًّا بعد تطبيق الـmigration، وأُثبتت النتائج عبر استعلامات `psql` مباشرة لا افتراضًا من الكود (تفاصيل في قسم "الإثبات الحي" أدناه).

## جولة مقارنة مع ChatGPT (C6 تحديدًا)

راجعتُ محاولة ChatGPT لـC6 عبر Agent بحثي مخصَّص قبل كتابة أي كود، بنفس المنهجية المتبعة في كل مرحلة سابقة.

**تحقّق إيجابي (وليس مجرد نسخ):**

استخدمت نسختهم فعليًا مكتبة `h3-js` الحقيقية لفهرسة الخلايا السداسية بدل رياضيات مُصنَّعة يدويًا، وصيغة Dispatch Score لديهم تطابق نص القسم 29.4 حرفيًا (الأوزان 0.40/0.20/0.15/0.10/0.15) — وهذا تحسّن حقيقي عن خطأهم في محاولة C4 (حيث دمجوا صيغة Dispatch Score قبل وجود محرك تغطية أصلًا، موثَّق في تقرير C4)، أي أن نفس الفريق طبّق حدود المراحل بعناية أكبر هذه المرة. حساب المتوسط/P90/أسوأ خلية لديهم يتبع بنية إحصائية سليمة (فرز تصاعدي ثم تراكم حتى عتبة النسبة المئوية) — تبنّيت نفس **البنية** (فرز + نسبة مئوية بالاستيفاء الخطي) في `percentile()`، مع رفض جزء آخر من نفس الحساب (التالي). كما أنهم حصروا نطاق C6 بالخط الأساسي فقط وأجّلوا صراحةً XGBoost وسيناريوهات "ماذا لو" لمراحل لاحقة في توثيقهم — يؤكد هذا استقلاليًا حدود هذا المشروع نفسها.

**فجوة حقيقية رُصدت وعُولجت بشكل أقوى (الأهم في هذه المقارنة):**

نموذج الخط الأساسي للطلب (§17، الموصوف بأنه "Baseline إلزامي") **غير موجود فعليًا** في نسختهم رغم الادّعاء. بدل حساب حقيقي، كتبوا اثنتي عشرة صفًا يدويًا مباشرة في بيانات البذر بصيغة `[lat, lon, predictedDemand]`، ووسموها `modelVersion: "weighted-baseline-v1"` وكأنها ناتج نموذج محسوب، مع نطاق ثقة ثابت مُختلَق `±28%` لا علاقة له بأي توزيع إحصائي حقيقي. هذا أخطر من تخطّي الميزة كليًا، لأن التسمية تدّعي وجود حساب لا وجود له — أي مستهلك لاحق (فريق العرض، مراجع، مطور آخر) سيثق ببيانات مصطنعة معروضة كأنها نتيجة نموذج. بنيت بدلًا منها `demand-baseline.ts`: خط أساس حقيقي من نوع "معدّل × مؤشر موسمي" (`predictedDemand = cellBaseRatePerHour × hourFactor × dayFactor`) محسوب فعليًا من `latitude/longitude/createdAt` الحقيقية لكل الحوادث التاريخية المزروعة، مع نطاق ثقة تقريبي مبني على تقريب بواسون-طبيعي (`z≈1.28`، `variance≈mean`) موسوم صراحة بأنه تقريبي — تمامًا كما يطلب النص نفسه. كما وثّقتُ صراحةً في أعلى الملف أن عوامل "المناسبات/أوقات الصلاة/الحرارة" المذكورة في النص **غير مُدرجة عمدًا** لعدم وجود بيانات مرجعية اصطناعية لها في هذا المشروع أصلًا — تطبيقًا لنفس مبدأ التحذير في القسم 18 ("لا تخترع بيانات غير متوفرة") بالقياس، لا اختراعها كما فعلت النسخة الأخرى.

جانب أصغر لكنه مقصود: حساب المتوسط/P90 للتغطية لديهم يُرجّح كل خلية بـ`predictedDemand` المُختلَقة أصلًا — أي أن مقياس التغطية نفسه يعتمد على بيانات طلب غير موثوقة. تعمّدتُ الإبقاء على `computeCoverageMetrics()` كمتوسط/نسبة مئوية إحصائية **غير مرجّحة** بالطلب، لفصل مقياس التغطية (موثوق، هندسي بحت) عن خط أساس الطلب (تقريبي، مُعلَّم كذلك بوضوح) — بنفس روح تحذير §18 حول عدم خلط بيانات غير مؤكدة بمقاييس ينبغي أن تبقى موثوقة.

## المرحلة C6 — الإرسال المدرك للتغطية: H3 / P90 / Dispatch Score (قسم 17/18/29.4)

الوحدات الجديدة:

- `src/lib/gis/h3.ts` — غلاف رقيق حول مكتبة `h3-js` الحقيقية (v4.5.0، مُثبَّتة عبر `npm install h3-js --save`). `H3_RESOLUTION = 8` هو خيار هذا المشروع الموثَّق (خلايا ~0.74 كم²، مناسبة لنطاق العرض التوضيحي المحدود حسب §19). يصدّر `latLngToH3Cell`, `h3CellToLatLng`, `h3GridDisk`, `isValidH3Cell`.
- `src/lib/gis/coverage.ts` — محرك فجوات التغطية. `computeCoverageMetrics()` يستدعي `RoutingProvider.matrix()` مرة واحدة (أصل=الوحدات، وجهة=مراكز خلايا H3)، ويُصنِّف الخلايا الأبعد من عتبة قابلة للتهيئة (افتراضيًا 480 ثانية/8 دقائق، خيار موثَّق لهذا المشروع) كفجوات. `computeCoverageProtection()` يحسب لكل وحدة أثر إزالتها من الأسطول على متوسط زمن الوصول (`meanEtaDeltaSeconds`)، ويحوّله إلى `protectionScore` بين 0-100 عبر سقف تدهور موثَّق (300 ثانية = درجة 0). يتطلب وحدتين على الأقل (وإلا فمفهوم "التغطية بدون هذه الوحدة" غير معرَّف)؛ مع وحدة واحدة فقط، يُسند `generateCoverageAwareRecommendation()` درجة حياد 100 موثَّقة صراحة (`SINGLE_UNIT_NO_COMPARISON`).
- `src/lib/gis/demand-baseline.ts` — خط الأساس الإلزامي لـ§17، مبني فعليًا من بيانات الحوادث التاريخية الحقيقية، لا أرقام مكتوبة يدويًا. تفاصيله وأسباب تصميمه في قسم المقارنة أعلاه.
- `src/lib/dispatch/dispatch-score.ts` — صيغة §29.4 مطابقة حرفيًا للنص (الأوزان تُتحقَّق مجموعها = 1 عند تحميل الوحدة). ملف منفصل تمامًا عن `access-score.ts` من C4 — لم يُعدَّل أي كود C4 مُختبَر ومُسلَّم مسبقًا؛ الفرقان الأساسيان: وزن ثقة الموقع ينخفض من 0.15 إلى 0.10، و"حداثة البيانات" (0.10 في C4) تُستبدَل بالكامل بـ"حماية التغطية" (0.15).
- `src/lib/dispatch/generate-coverage-recommendation.ts` — مولّد التوصية المدركة للتغطية. يعيد استخدام `computeUnitEntranceRoute()` من C4 (غُيِّر من دالة خاصة بالملف إلى دالة مُصدَّرة — تغيير إضافي بحت، بلا أي تعديل سلوكي، كل اختبارات C4 القائمة اجتازت دون تغيير) بدل تكرار منطق حساب المسار. نفس قاعدة كسر التعادل الموروثة من C4 (الدرجة تنازليًا، ثم زمن الوصول تصاعديًا، ثم جاهزية الوحدة تنازليًا). سطر التفسير (`reasoning`) يتضمن وسوم `COVERAGE_BEFORE:`/`COVERAGE_AFTER:` صريحة تسمح بمقارنة أثر القرار على التغطية العامة، لا مجرد درجة نهائية مجردة.

نموذج بيانات جديد في `schema.prisma` + `migration 0005_c6_coverage`:

- `H3Prediction` — جدول جديد وحيد هذه المرحلة (شبكي، بلا مفتاح أجنبي لحادثة/وحدة — يصف خلية جغرافية عبر الزمن لا حادثة بعينها). **ليس append-only** خلافًا لـ`LocationObservation`/`FieldAction` من مراحل سابقة، لأن إعادة حساب التوقعات عند إعادة البذر أمر مشروع هنا؛ يُدرَج بـ`ON CONFLICT DO UPDATE` عمدًا — قرار مبني مباشرة على درس C5 الموثَّق (تضارب زمني سببه استخدام `DO NOTHING` على جدول متغيّر عبر عمليات بذر بمعاملات مختلفة).
- جدول `Recommendation` (من C4) لم يحتج أي `ALTER TABLE`: أعمدته `accessScore`/`reasoning`/`scoreBreakdown` أُعيد استخدامها لحمل بيانات Dispatch Score أيضًا، والتمييز بينها وبين بيانات Access Score يتم عبر `algorithmVersion` (`access-score-v1` مقابل `dispatch-score-v1`) — تجنّبًا لأي خطر على مخطط C4 المُسلَّم مسبقًا.

## الاختبارات

انتقل عدد الاختبارات من 387 (نهاية C5) إلى **447** عبر هذه المرحلة — 60 اختبارًا جديدًا موزَّعة على:

- `h3.test.ts` (11 اختبارًا) — صحة التحويلات بين إحداثيات وخلايا H3، صلاحية الفهارس، الحتمية.
- `coverage.test.ts` (15 اختبارًا) — حساب المتوسط/P90/أسوأ خلية، تصنيف الفجوات حسب العتبة، حماية التغطية لكل وحدة، حالات الخطأ (لا وحدات/لا خلايا/وحدة واحدة فقط).
- `dispatch-score.test.ts` (8 اختبارات) — صحة الصيغة، التحقق من مجموع الأوزان، القص عند الحدود.
- `demand-baseline.test.ts` (9 اختبارات) — بناء النموذج، حساب المؤشرات الموسمية، صحة نطاق الثقة، الحتمية.
- `generate-coverage-recommendation.test.ts` (6 اختبارات) — يتضمن سيناريو اختبار القبول رقم 6 من §29.9 محسوبًا يدويًا بالضبط: وحدة أسرع وصولًا (90 ثانية) لكنها الوحيدة القريبة من خلية تغطية حرجة (60 ثانية) مقابل وحدة أبطأ (180 ثانية) لكن إزالتها من الأسطول لا تكلف التغطية شيئًا (3000 ثانية لنفس الخلية الحرجة) — الدرجتان محسوبتان يدويًا: السريعة 78، البطيئة 89؛ البطيئة تفوز، ما يثبت أن "الوحدة الأسرع التي تضرّ بالتغطية" يمكن أن تخسر مع تفسير واضح للسبب، تمامًا كما ينص معيار القبول.
- `seed-coverage-fixtures.test.ts` (11 اختبارًا) — يثبت أن مسار C1→C2→C3→C4→C6 الكامل يعمل بتشغيل حقيقي للخوارزميات: شبكة خلايا صالحة وفريدة وحتمية، عدد توقعات H3 يطابق (خلايا × نوافذ زمنية)، نطاقات ثقة منطقية، توصية التغطية معلوماتية بحتة (لا قبول/رفض)، تضمين وسوم `COVERAGE_BEFORE`/`COVERAGE_AFTER`، الحتمية، ورسالة خطأ واضحة عند غياب الحادثة الهدف.

## عثرات حقيقية أثناء البناء (وإصلاحها)

- **`coverage.test.ts`**: اختبار "يحدّد الخلية الأسوأ فعليًا (أعلى زمن وصول)" كان يمنح الوحدة الثانية صفًا ثابتًا `[900,900,900]`، فجاء أدنى زمن وصول لخلية `cell-c` فعليًا `900` (من الوحدة الثانية) لا `5000` كما كان القصد — الاختبار فشل بـ`expected 900 to be 5000`. الإصلاح: تغيير قيمة الوحدة الثانية لتلك الخلية إلى `6000` بحيث يكون `min(5000,6000)=5000` هو الأسوأ فعليًا عبر الوحدتين.
- **`demand-baseline.test.ts`**: اختبار المؤشر الموسمي الساعي استخدم في البداية ساعتين مأهولتين فقط (10 حوادث في الساعة 14، وحادثة واحدة في الساعة 3 من أصل 24 ساعة ممكنة)، ما جعل المتوسط لكل ساعة `11/24≈0.458` — فحتى الساعة "الهادئة" جاء مؤشرها `1/0.458≈2.18 > 1`، وفشل الاختبار بـ`expected 2.18 to be less than 1`. الإصلاح: إعادة كتابة توليد الحوادث بحيث تحصل كل ساعة من الـ24 على خلفية مسطّحة من 5 حوادث، ثم رفع الساعة 14 إلى 20 وخفض الساعة 3 إلى 1 — بحيث تصبح "المزدحمة"/"الهادئة" ذات معنى نسبةً لمتوسط واقعي.
- **خطأ ESLint**: واجهة غير مستخدَمة `CellAggregate` بقيت في `demand-baseline.ts` بعد أن حلّت محلّها `DemandBaselineModel` أثناء الكتابة، ما تسبب في خطأ `@typescript-eslint/no-unused-vars` أوقف `npm run verify` عند خطوة lint. الإصلاح: حذف الواجهة الميتة.

## الإثبات الحي (psql مباشرة بعد `npm run seed`)

```sql
SELECT COUNT(*) FROM "H3Prediction";
-- 222

SELECT "modelVersion", COUNT(DISTINCT "h3Index") AS cells, COUNT(*) AS rows
FROM "H3Prediction" GROUP BY "modelVersion";
-- h3-baseline-v1 | 37 | 222   (37 خلية × 6 نوافذ ساعية = 222، يطابق الكود تمامًا)

SELECT synthetic, COUNT(*) FROM "H3Prediction" GROUP BY synthetic;
-- t | 222   (كل الصفوف synthetic=true، بلا استثناء)

SELECT id, "algorithmVersion", "recommendedUnitId", "acceptedById", "acceptedAt", "rejectedAt"
FROM "Recommendation" WHERE "incidentId" = 'inc-active-03' ORDER BY "createdAt";
-- rec-inc-active-03          | access-score-v1   | unit-10 | user-supervisor | 2026-08-28 11:23:43 | NULL   (صف C4 الأصلي، لم يُمسّ)
-- rec-coverage-inc-active-03 | dispatch-score-v1 | unit-04 | NULL            | NULL                | NULL   (صف C6 الجديد، معلوماتي بحت كما صُمِّم)
```

صف C6 يحمل `acceptedById`/`acceptedAt`/`rejectedAt`/`overrideReason` كلها `NULL` كما هو مصمَّم (توصية محاكاة، لم تُدفَع عبر `decideDispatch()` لأن الحادثة أُغلقت فعليًا في C5) — وصف C4 الأصلي (المقبول فعليًا، `unit-10`) لم يتأثر إطلاقًا، ما يثبت أن إعادة استخدام أعمدة `Recommendation` لم تُحدث أي تصادم بين المرحلتين. حقل `reasoning` للتوصية الجديدة يحتوي فعليًا `COVERAGE_BEFORE:mean=481s,p90=678s,worstCell=885373699dfffff(763s),gaps=20/37,mode=simulation` و`COVERAGE_AFTER:mean=815s,p90=1049s,...,gaps=37/37,mode=simulation` — أرقام منطقية: إزالة `unit-04` من أسطول العرض التوضيحي الصغير (10 وحدات على 37 خلية) تُضعف التغطية بوضوح (من 20 إلى 37 خلية متجاوزة للعتبة)، ما يفسّر لماذا حصلت `coverageProtection` على درجة منخفضة (0) لهذه الوحدة تحديدًا في تفصيل الدرجة.

## ما تبقى (لم يُبدأ، حسب حدود المراحل المتفق عليها)

- **واجهة Next.js فعلية**: كل ما بُني حتى الآن (C1–C6) منطق خلفي مُختبَر بالكامل بلا صفحات — القرار المعلَّق نفسه من التقارير السابقة لا يزال بانتظار توجيهك.
- **قناة استقبال HTTP فعلية** لتوصيات C6 والتحديثات الميدانية من C5 — لا يوجد بعد Route/Endpoint في الخادم، ضمن نطاق واجهة Next.js أعلاه.
- C7 (صحة التكامل، fallback، أمان، E2E) وC8 (جاهزية المستشفى — لا تُبدأ قبل نجاح C0–C7) وC9 (الإجهاض/التصدير — لا تُبدأ إلا إذا استقر سيناريو العرض) — لم تبدأ. سأتبع نفس انضباط "مرحلة بمرحلة" عند الوصول إليها، بدءًا من C7 في الجولة القادمة.

## نقل التحديث لجهازك

الملف المضغوط الجديد يحتوي المشروع كاملًا (C1+C2+C3+C4+C5+C6، بدون node_modules). فك الضغط فوق `E:\projects\najat360` الحالي (أو مكان جديد)، ثم:
```
npm install
cp .env.example .env
# عدّل DATABASE_URL
psql "$DATABASE_URL" -f prisma/migrations/0001_init/migration.sql
psql "$DATABASE_URL" -f prisma/migrations/0002_c3_location/migration.sql
psql "$DATABASE_URL" -f prisma/migrations/0003_c4_dispatch/migration.sql
psql "$DATABASE_URL" -f prisma/migrations/0004_c5_fieldlink/migration.sql
psql "$DATABASE_URL" -f prisma/migrations/0005_c6_coverage/migration.sql
npm run seed
npm run verify
```
